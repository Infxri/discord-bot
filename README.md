# Discord Music + RPG + Gambling Bot

## ⚙️ Features
- 🎵 Play music from YouTube/Spotify links or keywords
- ⚔️ Text-based RPG system (PvE + leveling + leaderboards)
- 💰 Simple gambling & currency system
- 🧠 Command help list
- 💤 Stays idle in VC (like professional bots)

## 🚀 Run locally
1. Create a file `.env`:

```
DISCORD_TOKEN=your_bot_token_here
```

2. Install dependencies:

```
npm install
```

3. Start the bot:

```
npm start
```

## 🌐 Deploy to Render
- Connect this repo to [https://render.com](https://render.com)
- Build command: `npm install`
- Start command: `npm start`
- Add env var: `DISCORD_TOKEN` = your token